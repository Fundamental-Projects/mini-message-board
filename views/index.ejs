<!-- index.ejs -->
<html>
  <head>
    <title><%= title %></title>
  </head>
  <body>
    <div>
      <%- include("form") %>
    </div>
    <ul>
      <% messages.forEach((message,i) => { %>
        <%- include('messages/message', {user: message.user, message:message.text, date:message.added, index:i}); %>
      <% }); %>

    </ul>

    <%- include('footer') %>

  </body>
</html>
