<!-- index.ejs -->
<html>
  <head>
    <title><%= title %></title>
  </head>
  <body>
    <div>
      <%- include("form") %>
    </div>
    <ul>
      <% messages.forEach((message) => { %>
        <%- include('messages/message', {user: message.user, message:message.text, date:message.added}); %>
      <% }); %>

    </ul>

    <%- include('footer') %>

  </body>
</html>
